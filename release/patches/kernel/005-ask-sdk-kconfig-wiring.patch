From: ASK build tooling <ask@localhost>
Subject: [PATCH] ASK: wire NXP SDK DPAA/FMan/QBMan Kconfig & Makefile into parents

The NXP "SDK" DPAA stack is dropped into the tree by apply-to-tree.sh as:

    drivers/staging/fsl_qbman/                       (BMan/QMan)
    drivers/net/ethernet/freescale/sdk_dpaa/         (SDK DPAA Ethernet)
    drivers/net/ethernet/freescale/sdk_fman/         (SDK Frame Manager)

Each of those subtrees carries its own Kconfig and top-level Makefile,
but the parent Kconfig / Makefile files do not reference them. Without
this wiring, symbols like CONFIG_FSL_SDK_DPA, CONFIG_FSL_SDK_FMAN and
CONFIG_FSL_SDK_DPAA_ETH are invisible to merge_config.sh and
olddefconfig, so they are silently dropped from the final .config
regardless of what release/ask.config requests.

This patch adds the missing `source` directives and `obj-$(...)` hooks
to the parent Kconfig/Makefile of both locations so that the SDK
symbols become resolvable during config merge and are built when
enabled.

This is a build-system-only change; no code logic is modified.

Signed-off-by: ASK build tooling <ask@localhost>
---
diff -urN a/drivers/net/ethernet/freescale/Kconfig b/drivers/net/ethernet/freescale/Kconfig
--- a/drivers/net/ethernet/freescale/Kconfig
+++ b/drivers/net/ethernet/freescale/Kconfig
@@ -107,4 +107,7 @@
 source "drivers/net/ethernet/freescale/dpaa2/Kconfig"
 source "drivers/net/ethernet/freescale/enetc/Kconfig"
 
+source "drivers/net/ethernet/freescale/sdk_dpaa/Kconfig"
+source "drivers/net/ethernet/freescale/sdk_fman/Kconfig"
+
 endif # NET_VENDOR_FREESCALE
diff -urN a/drivers/net/ethernet/freescale/Makefile b/drivers/net/ethernet/freescale/Makefile
--- a/drivers/net/ethernet/freescale/Makefile
+++ b/drivers/net/ethernet/freescale/Makefile
@@ -25,3 +25,6 @@
 obj-$(CONFIG_FSL_DPAA2_ETH) += dpaa2/
 
 obj-y += enetc/
+
+obj-$(CONFIG_FSL_SDK_DPAA_ETH) += sdk_dpaa/
+obj-$(CONFIG_FSL_SDK_FMAN) += sdk_fman/
diff -urN a/drivers/staging/Kconfig b/drivers/staging/Kconfig
--- a/drivers/staging/Kconfig
+++ b/drivers/staging/Kconfig
@@ -76,4 +76,6 @@
 
 source "drivers/staging/vme_user/Kconfig"
 
+source "drivers/staging/fsl_qbman/Kconfig"
+
 endif # STAGING
diff -urN a/drivers/staging/Makefile b/drivers/staging/Makefile
--- a/drivers/staging/Makefile
+++ b/drivers/staging/Makefile
@@ -27,3 +27,5 @@
 obj-$(CONFIG_XIL_AXIS_FIFO)	+= axis-fifo/
 obj-$(CONFIG_FIELDBUS_DEV)     += fieldbus/
 obj-$(CONFIG_QLGE)		+= qlge/
+
+obj-$(CONFIG_FSL_SDK_DPA) += fsl_qbman/
