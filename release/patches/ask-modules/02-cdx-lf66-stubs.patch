--- a/cdx/cdx_common.h
+++ b/cdx/cdx_common.h
@@ -43,7 +43,7 @@
 #endif
 
 //include ethernet fast path statistics
-#define INCLUDE_ETHER_IFSTATS 1
+//#define INCLUDE_ETHER_IFSTATS 1  /* ask10: lf-6.6.y SDK has no dpa_set_eth_ifinfo() / dpa_update_eth_if() */
 
 #if defined(INCLUDE_VLAN_IFSTATS) || defined(INCLUDE_PPPoE_IFSTATS) || defined(INCLUDE_TUNNEL_IFSTATS) || defined(INCLUDE_ETHER_IFSTATS)
 #define INCLUDE_IFSTATS_SUPPORT 1
--- a/cdx/cdx_ehash.c
+++ b/cdx/cdx_ehash.c
@@ -105,7 +105,7 @@
 #endif
 static int insert_remove_vlan_hm(struct ins_entry_info *info, uint32_t iif_index, uint32_t underlying_iif_index);
 static int create_vlan_ins_hm(struct ins_entry_info *info);
-static int create_eth_rx_stats_hm(struct ins_entry_info *info, uint32_t iif_index, uint32_t underlying_iif_index);
+static int __maybe_unused create_eth_rx_stats_hm(struct ins_entry_info *info, uint32_t iif_index, uint32_t underlying_iif_index);
 static int cdx_create_fragment_bufpool(void);
 #ifdef ENABLE_INGRESS_QOS
 static int create_preemptive_checks_hm(struct ins_entry_info *info,uint16_t queue_no);
@@ -2546,7 +2546,7 @@
 	return SUCCESS;	
 }
 
-static int create_eth_rx_stats_hm(struct ins_entry_info *info, uint32_t iif_index, uint32_t underlying_iif_index)
+static int __maybe_unused create_eth_rx_stats_hm(struct ins_entry_info *info, uint32_t iif_index, uint32_t underlying_iif_index)
 {
 #ifdef INCLUDE_ETHER_IFSTATS
 	uint8_t offset;
@@ -3298,8 +3298,9 @@
 	while (buffer_count < CDX_FRAG_BUFFERS_CNT)
 	{
 		refill_cnt = 0;
-		ret = dpaa_eth_refill_bpools(bp, &refill_cnt,
-			CONFIG_FSL_DPAA_ETH_REFILL_THRESHOLD);
+		/* ask12: lf-6.6.y dpaa_eth_refill_bpools() takes (dpa_bp, count_ptr) only;
+		 * CONFIG_FSL_DPAA_ETH_REFILL_THRESHOLD threshold arg was lf-6.12.y-only. */
+		ret = dpaa_eth_refill_bpools(bp, &refill_cnt);
 		if (ret < 0)
 		{
 			DPA_ERROR("%s:: Error returned for dpaa_eth_refill_bpools %d\n", __FUNCTION__,ret);
--- a/cdx/devman.c
+++ b/cdx/devman.c
@@ -830,7 +830,9 @@
 #endif
 		}
 		else if (iface->if_flags & IF_TYPE_ETHERNET ) {
+#ifdef INCLUDE_ETHER_IFSTATS
 			l2_info->ether_stats_offset = iface->txstats_index;
+#endif
 			l2_info->dscp_vlanpcp_map_enable = 
 				cdx_get_tx_dscp_vlanpcp_map_enable(iface->eth_info.portid);
 			if ((l2_info->dscp_vlanpcp_map_enable) &&
@@ -2690,12 +2692,16 @@
 
 void dpa_update_timestamp(uint32_t ts)
 {
-	if (!extHashTsInfo.ptr)
-		return;
-	FM_PCD_UpdateExtTimeStamp(EXTERNAL_TIMESTAMP_TIMERID, cpu_to_be32(ts));
+	/* ask10: lf-6.6.y SDK has no external-timestamp / EHASH support.
+	 * extHashTsInfo, FM_PCD_UpdateExtTimeStamp, EXTERNAL_TIMESTAMP_TIMERID
+	 * do not exist. Stub to no-op; PTP/external-timestamp is not used
+	 * by the boot path. */
+	(void)ts;
 }
 
 uint32_t dpa_get_timestamp_addr(uint32_t id)
 {
-	return(FM_PCD_GetExtTimeStampAddr(id));
+	/* ask10: see dpa_update_timestamp(). */
+	(void)id;
+	return 0;
 }
--- a/cdx/devoh.c
+++ b/cdx/devoh.c
@@ -516,7 +516,12 @@
 			}
 		} else {
 			printk(KERN_CRIT "%s::freeing sg buffers now\n", __FUNCTION__);
+			/* ask13: lf-6.6.y declares dpa_fd_release() with __attribute__((nonnull));
+			 * the offline-port RX-error path legitimately has no associated netdev. */
+			#pragma GCC diagnostic push
+			#pragma GCC diagnostic ignored "-Wnonnull"
 			dpa_fd_release(NULL, fd);
+			#pragma GCC diagnostic pop
 		}
 	}
 	return qman_cb_dqrr_consume;
--- a/cdx/cdx_ceetm_app.c
+++ b/cdx/cdx_ceetm_app.c
@@ -19,6 +19,15 @@
 #include "cdx_ceetm_gdef.h"
 #include "cdx_common.h"
 
+/* ask14: lf-6.6.y SDK does not export CEETM helpers added in lf-6.12.y.
+ * CEETM is TX-QoS shaping; not on the boot path / cmm path. Stub the
+ * 4 missing externs as no-ops so the cdx OOT module links. */
+static inline uint16_t FmPcdPlcrProfileGetAbsoluteId(void *h) { (void)h; return 0; }
+static inline int dpa_register_ceetm_get_egress_fq(void *a, void *b) { (void)a; (void)b; return 0; }
+static inline void dpa_enable_ceetm(struct net_device *nd) { (void)nd; }
+static inline int qman_ceetm_configure_mapping_shaper_tcfc(void *o) { (void)o; return 0; }
+
+
 static struct ceetm_chnl_info qm_chnl_info[CDX_CEETM_MAX_CHANNELS];
 
 
