# lts_6.6_ls1046a — Agent Rules

Producer repo for the ASK kernel (`kernel-6.6.137-askN`). Builds Linux 6.6.137 + VyOS patches + NXP SDK DPAA/FMan/QBMan drivers and publishes a GitHub Release for the consumer `vyos-ls1046a-build` to pin against.

## Critical Build Workflow Rules

### Push the TAG ONLY for ASK iterations

For each new ask kernel iteration:

```bash
# Edit patches/code, run patch-health.sh --source release locally first
git commit -am '...'
git tag kernel-6.6.137-askN
git push origin kernel-6.6.137-askN      # ← TAG ONLY
```

**Do NOT** also push `lts-6.6-ls1046a` in the same `git push` command. The workflow triggers on BOTH ref types:

| Trigger | Behavior |
|---|---|
| `push: branches: [lts-6.6-ls1046a, main]` | Safety-net CI build, **no release** |
| `push: tags: [kernel-*]` | Build + **publishes Release** |

`git push origin lts-6.6-ls1046a kernel-6.6.137-askN` fires the workflow twice — once for the branch, once for the tag — wasting ~22 min of GitHub Actions minutes on a discarded redundant build. The `concurrency` group does not deduplicate them because branch and tag refs are distinct.

Push the branch ref only when you have **non-release** commits worth a CI sanity check (tooling, AGENTS.md, README, scripts) — and in that case do it BEFORE you cut the tag, in a separate push.

### Always run patch-health locally before tagging

```bash
rm -rf work/linux-6.6.137 && tar -xf work/linux-6.6.137.tar.xz -C work/
bash scripts/patch-health.sh --source release
```

Must report `Pass: 25   Fail: 0` and `0 SDK conflicts (266 files to install)` (post-ask25 baseline). A clean `patch-health` is **not sufficient** — `git apply` may report success even when a patch's hunk count is wrong and lines get silently truncated. After patch-health, also visually inspect the affected file:

```bash
patch -p1 -d work/linux-6.6.137 < release/patches/ask/0X0-…patch
grep -n <expected-content> work/linux-6.6.137/<patched-file>
```

This caught the ask13 → ask14 hunk-count bug where `@@ -25,3 +25,6 @@` truncated 12 added lines to 6, silently dropping `obj-$(CONFIG_FSL_SDK_FMAN)` and `obj-$(CONFIG_FSL_SDK_DPAA_ETH)` from the Makefile, producing a kernel with neither sdk_fman nor sdk_dpaa built.

### Workflow file

`.github/workflows/build-and-release.yml` runs on `ubuntu-24.04-arm` (GitHub-hosted ARM64). Native build, ~22 min. **Tag pushes** publish a GitHub Release; **branch pushes** do not.

## Patch Inventory

Patches are organised in three buckets that mirror ASK-mono. Apply order
is `vyos/` → `ask/` → `fixes/`; within each bucket, sort by filename.

### `release/patches/vyos/` — VyOS deltas (apply first)

| # | Patch | Purpose |
|---|---|---|
| 001 | `vyos-linkstate-ip-device-attribute.patch` | VyOS link-state attr |
| 002 | `vyos-inotify-stackable-filesystems.patch` | VyOS inotify on overlayfs |
| 003 | `vyos-build-linux-perf-package.patch` | linux-perf .deb |

### `release/patches/ask/` — ASK fast-path (ASK-mono buckets, 010..080)

| # | Patch | Purpose |
|---|---|---|
| 010 | `ask-fman-dpaa-ehash.patch` | FMan/DPAA misc + SDK Kconfig+Makefile wiring |
| 020 | `ask-bridge-hooks.patch` | Bridge fast-path hooks (`abm_ff`, brevent notifier) |
| 030 | `ask-ipv4-ipv6-forwarding.patch` | IPv4/IPv6 forwarding fast-path |
| 040 | `ask-xfrm-ipsec-offload.patch` | IPsec offload (gated by `INET_IPSEC_OFFLOAD`) |
| 050 | `ask-conntrack-offload.patch` | Conntrack offload (`fp_info`, `qosconnmark`) |
| 060 | `ask-netfilter-qosmark.patch` | `comcerto_fp_netfilter.c` + xt_QOSMARK/QOSCONNMARK |
| 070 | `ask-ppp-hooks.patch` | PPP fast-path hooks |
| 080 | `wext-core-restore-ndo_do_ioctl.patch` | Wireless-extensions core restore |

### `release/patches/fixes/` — 6.6.y-specific repairs (090+)

| # | Patch | Purpose |
|---|---|---|
| 093 | `netlink-name-L2FLOW-cb-mutex.patch` | Lockdep mutex name (avoids dup name with NETLINK_GENERIC) |
| 094 | `swphy-10g-fixed-link.patch` | 10G fixed-link swphy support |
| 095 | `leds-lp5812-register.patch` | Register the lp5812 LED driver in `drivers/leds/Makefile` + `Kconfig` |
| 097 | `ask-fci-nlkey-narrow-gate.patch` | Adds `net/key/ask_fci_nlkey.c` + `CONFIG_ASK_FCI_NLKEY` to register `NETLINK_KEY=32` without enabling the (broken-on-6.6) IPsec offload data path |
| 099 | `dpaa-ethtool-quiet-no-phy.patch` | Demote the six remaining `netdev_err("phy device not initialized")` call sites in `sdk_dpaa/dpaa_ethtool.c` (`dpa_set_ksettings`, `dpa_nway_reset`, `dpa_get_pauseparam`, `dpa_set_pauseparam`, `dpa_get_eee`, `dpa_set_eee`) to `netdev_dbg`, matching the pre-existing pattern at the other two callsites. Boards with fixed-link / SFP+ cages (no `phylink`) legitimately have `mac_dev->phy_dev == NULL`; PHY-only ethtool ioctls were filling dmesg with dozens of identical KERN_ERR lines per VyOS interface commit. `-ENODEV` returns are unchanged so userspace still sees the op as unsupported |
| 101 | `stdlib-ext-fortify-port.patch` | (ask7) Drop 5 string prototypes (`strlen`, `strnlen`, `strcpy`, `strncpy`, `strtok`) from `sdk_fman/inc/stdlib_ext.h`. Mainline 6.6 defines these as `__builtin_choose_expr` macros via `<linux/fortify-string.h>`; redeclaring them as plain functions makes any TU that includes both fail with `expected identifier or '(' before '__builtin_choose_expr'` at `fortify-string.h:218`. Required for the lf-6.6.y SDK overlay to compile against stock `linux-6.6.137` |
| 102 | `arm64-ioremap-cache-ns-shim.patch` | (ask7, extended ask8) Two NXP-private arm64 helper aliases in `arch/arm64/include/asm/io.h`: `ioremap_cache_ns()` → `ioremap_cache()` (used by `bman_driver.c:194`, `qman_driver.c:451`) and `pgprot_cached_ns(prot)` → identity (used by `fsl_usdpaa.c:829`). NXP's lf-6.x defines both via `PROT_NORMAL_NS` / `PTE_NS` (Non-Secure TLB attr); mainline lacks both helpers and the attr. On this SoC running in EL2 the portals never traverse secure-world, so the cacheable / unchanged-prot mapping is a strict superset. Required for the lf-6.6.y SDK overlay to compile |
| 103 | `sdk-fman-ncsw-min-max-guard.patch` | (ask8) Guard `MIN()` / `MAX()` macros in `sdk_fman/inc/ncsw_ext.h` with `#ifndef`. Mainline 6.6 added `<linux/minmax.h>` to several headers transitively included by the SDK; redefinition produces `-Werror=macro-redefined` |
| 104 | `sdk-dpaa-mac-phylink-shims.patch` | (ask8) Two compile-time shims at the top of `sdk_dpaa/mac.c`: `#define PHY_INTERFACE_MODE_2500SGMII PHY_INTERFACE_MODE_2500BASEX` (NXP renamed in lf-6.x) and a static-inline `phylink_interface_max_speed()` returning `SPEED_10000` / `SPEED_2500` / `SPEED_1000` / `SPEED_100`. Mainline 6.6 lacks both; the SDK calls them in port-init paths gated by phylink |
| 105 | `sdk-dpaa-skb-recycle-shim.patch` | (ask8) Static-inline `skb_recycle()` shim at the top of `sdk_dpaa/dpaa_eth_sg.c`. The helper was removed from mainline before 6.6; SDK fast-path RX still calls it. Replicates the original semantics (release head state, zero shinfo up to dataref, set dataref=1, zero skb up to tail) |
| 106 | `sdk-export-symbols-for-cdx-oot.patch` | (ask18) `EXPORT_SYMBOL` for 5 lf-6.6.y SDK helpers consumed by the cdx OOT module: `FM_PORT_PcdPlcrAllocProfiles`, `FM_VSP_Config`, `FM_VSP_ConfigBufferPrefixContent`, `FM_MURAM_AllocMem`, and `EXPORT_SYMBOL_GPL` for `qman_sp_enable_ceetm_mode`. Pure parity with lf-6.12.y's export set — not inventing new exports |
| 107 | `qbman-dpa_alloc-preallocate-nodes.patch` | (ask20, upstream `we-are-mono/ASK` `fix/security-hardening`@2f73756) `staging/fsl_qbman/dpa_alloc_new`: preallocate the three candidate list nodes BEFORE taking `spin_lock_irq(&alloc->lock)`. The old code did `kmalloc(GFP_KERNEL)` inside the spinlock (sleeping in atomic context); `CONFIG_DEBUG_ATOMIC_SLEEP` flagged it on every fqalloc / cgralloc / bmpalloc / ceetm0_challoc path on boot. No functional change, no ABI delta — just moves the alloc out of the critical section |
| 108 | `sdk_dpaa-dpa_get_channel-use-mutex.patch` | (ask20, upstream `f48a9f3`) `sdk_dpaa/dpaa_eth_common.c::dpa_get_channel`: spinlock → mutex around the one-shot `qman_alloc_pool_range()` init. The alloc sleeps via `kmalloc(GFP_KERNEL)`; the spinlock would deadlock under memory pressure. Init only runs in process context during probe, so a mutex is the correct primitive |
| 109 | `sdk_fman-FmPcdLockTryLockAll-nest-annotation.patch` | (ask20, upstream `ab4ef45`) `sdk_fman` lockdep nest annotation. Adds `XX_LockIntrSpinlockNested(h, subclass)` and `FmPcdLockTryLockNested`; uses `SINGLE_DEPTH_NESTING` at the inner site in `FmPcdLockTryLockAll` so the outer `h_FmPcd->h_Spinlock` and per-object inner locks (which all share the same lockdep class because they're allocated via `XX_InitSpinlock`) don't trip the recursive-locking warning that fires on every `dpa_app → FM_PORT_SetPCD → LnxwrpFmPortIOCTL` path |
| 110 | `sdk-fman-dpaa-qbman-kasan-sanitize-off.patch` | (ask20, upstream `a20c2d9`) Adds `KASAN_SANITIZE := n` to the Makefiles for `sdk_fman/`, `sdk_dpaa/`, `staging/fsl_qbman/`. The vendored NXP trees do plain `memset()/memcpy()` against iomem (devm_ioremap'd MURAM SRAM, QBMan portals); with `CONFIG_KASAN_GENERIC=y` the compiler rewrites these to instrumented variants that perform a shadow-byte lookup on the destination, which faults because iomem has no KASAN shadow mapped. Surfaced as a boot panic during `FM_PCD_Init → AllocFEObjs`. Scopes KASAN to code we own (cdx, fci, auto_bridge) while keeping global instrumentation on for everything else |
| 111 | `sdk_fman-mac-hash-alloc-null-check.patch` | (ask20, upstream `053a322`) `sdk_fman/MemacAddHashMacAddress`: NULL-check the result of `XX_Malloc` and return `E_NO_MEMORY`. The path is reachable from any `dev_mc_add()` against an FMAN-backed netdev (IGMP joins, IPv6 ND, mcast-offload ADD); on `XX_Malloc` failure (ENOMEM under pressure / fault injection) the next line was a NULL-deref panic |
| 112 | `uapi-fm-pcd-hashtable-extend.patch` | (ask24) Extend `ioc_fm_pcd_hash_table_params_t` (UAPI) and mirror `t_FmPcdHashTableParams` (LLD) with the `DPAA_VERSION>=11` extension fields: `aging_support` (always), `external_hash` + IPR-offload sub-struct (`!EXCLUDE_FMAN_IPR_OFFLOAD`) + `external_hash_params{data_mem_id, data_liodn_offs, miss_monitor_addr}`. Brings `sizeof(ioc_fm_pcd_hash_table_params_t)` from 64 → 120 and `FM_PCD_IOC_HASH_TABLE_SET` from `0xc040e139` → `0xc078e139` so it matches what ASK fmlib/`fmc`/`dpa_app` (built against lf-6.12.y / nxp-linux UAPI with DPAA_VERSION=11) issues. Without this, `LnxwrpFmPcdIOCTL` falls into the `default:` branch with "Invalid Selection", `dpa_app applied PCD configuration (failed rc=65300)`, and `cdx_create_fragment_bufpool::failed to locate eth bman pool` — even though the kernel itself is DPAA_VERSION=11. Caveat: this unblocks the IOCTL gate only; lf-6.6.y `FM_PCD_HashTableSet()` does NOT consume the new fields, so hash tables still allocate in MURAM. If `cdx_pcd.xml` exceeds 384 KiB MURAM, `AllocStatsObjs` is the next failure — that is a separate Chain-2 (consumer) issue |

> **ask7/ask8 build fixes (2026-05-03):** the lf-6.6.y SDK overlay required several thin compat shims to compile against stock `linux-6.6.137` (lf-6.6.y itself ships ~6.6.52 + NXP-private API extensions). ask7 introduced `fixes/101` and `fixes/102` (stdlib_ext fortify port + ioremap_cache_ns alias). ask7 CI then surfaced four more lf-6.6.y vs mainline-6.6.137 collisions covered by ask8: `pgprot_cached_ns` (folded into `fixes/102`), `MIN/MAX` redefinition (`fixes/103`), `phylink_interface_max_speed` + `PHY_INTERFACE_MODE_2500SGMII` (`fixes/104`), and `skb_recycle` (`fixes/105`). Whole-tree wet-run of `sdk_dpaa/`, `sdk_fman/`, `fsl_qbman/` produced 74 `.o` files with 0 errors. Shims are minimal and isolated — no behaviour change for non-SDK paths.

> **ask9 OOT module fix (2026-05-03):** ask8 CI built the kernel + SDK overlay clean but failed at the `--ask-extras` step (`scripts/build-ask-modules.sh`) with `cdx_common.h:18:10: fatal error: fm_ehash.h: No such file or directory`. The OOT `cdx` module (sourced from `we-are-mono/ASK` via `git archive`, 8 TUs `#include "fm_ehash.h"`) needs the `en_exthash_*` type definitions even though the kernel-side `FM_PCD_HashTableSet()` in lf-6.6.y never takes the EHASH branch. ask6 over-deleted the headers; ask9 restores **3 EHASH headers only** (no `.c`) by copying from the 6.6 reference (`mihakralj/ask-ls1046a-6.6` `main`): `fm_ehash.h` (1690 lines), `fm_eh_types.h` (52 lines), `fm_cc_dbg.h` (1378 lines). The kernel-side `Pcd/Makefile` from lf-6.6.y already does NOT list `fm_ehash.o`, so no kernel-side EHASH code is linked — Chain 2-B remains structurally unreachable per the ask6 doctrine. SDK file count: 262 → 265.

> **ask10 OOT module fix (2026-05-03):** ask9 CI got past the missing-header error but failed at compile of `work/ask-oot/src/cdx/devman.c` with five `-Werror=implicit-function-declaration` / `undeclared` errors against symbols only present in the lf-6.12.y SDK overlay used by ASK-mono — not in lf-6.6.y. Three of these are external-timestamp / EHASH (`extHashTsInfo`, `FM_PCD_UpdateExtTimeStamp`, `FM_PCD_GetExtTimeStampAddr`); the other two are sdk_dpaa helpers (`dpa_set_eth_ifinfo`, `dpa_update_eth_if`) that lf-6.12.y added but lf-6.6.y lacks. Fix: a new ask-modules patch `release/patches/ask-modules/02-cdx-lf66-stubs.patch` (a) comments out `#define INCLUDE_ETHER_IFSTATS 1` in `cdx/cdx_common.h` (the `dpa_set_eth_ifinfo` / `dpa_update_eth_if` calls live entirely inside `#ifdef INCLUDE_ETHER_IFSTATS` blocks and have no users elsewhere; the `INCLUDE_IFSTATS_SUPPORT` umbrella stays defined via VLAN/PPPoE/TUNNEL paths so `cdx_ifstats.c` keeps compiling); (b) stubs the bodies of `dpa_update_timestamp()` and `dpa_get_timestamp_addr()` in `cdx/devman.c` to no-ops, removing the references to `extHashTsInfo` / `FM_PCD_UpdateExtTimeStamp` / `FM_PCD_GetExtTimeStampAddr` / `EXTERNAL_TIMESTAMP_TIMERID`. PTP / external-timestamp is not used on the boot path. No kernel-side patch changes.

> **ask11 OOT module fix (2026-05-03):** ask10 CI advanced past the previous five errors but stopped at `work/ask-oot/src/cdx/devman.c:833` with `error: 'struct dpa_l2hdr_info' has no member named 'ether_stats_offset'`. The struct field at `cdx/cdx_common.h:257` is wrapped in `#ifdef INCLUDE_ETHER_IFSTATS`, so once ask10 commented that macro out the field disappeared. Three of the four `l2_info->ether_stats_offset = ...` writers in `devman.c` are correctly under `#ifdef INCLUDE_ETHER_IFSTATS` (lines 1108/1275/1526 in the post-ask10 numbering) but the writer at line 833 (inside the `IF_TYPE_ETHERNET` branch of `cdxdrv_get_l2hdr_info`) was missing the gate. Fix: extend `release/patches/ask-modules/02-cdx-lf66-stubs.patch` to wrap that single line in `#ifdef INCLUDE_ETHER_IFSTATS` / `#endif`, matching the pattern already in use at the three other write sites. Verified by `grep -nE "ether_stats_offset" cdx/devman.c` post-apply: all 4 writers preceded by an `#ifdef INCLUDE_ETHER_IFSTATS` line.


> **ask12 OOT module fix (2026-05-03):** ask11 CI cleared `devman.c` but failed in `work/ask-oot/src/cdx/cdx_ehash.c` with two errors: (1) `cdx_ehash.c:3301: error: too many arguments to function 'dpaa_eth_refill_bpools'` and (2) `cdx_ehash.c:2549: error: 'create_eth_rx_stats_hm' defined but not used [-Werror=unused-function]`. (1) lf-6.6.y `dpaa_eth_refill_bpools()` has signature `(struct dpa_bp *, int *count_ptr)` (confirmed in `release/patches/kernel/sdk-sources/drivers/net/ethernet/freescale/sdk_dpaa/dpaa_eth_sg.c:185` and `dpaa_eth.h:432`); the cdx caller had a third lf-6.12.y-only arg `CONFIG_FSL_DPAA_ETH_REFILL_THRESHOLD`. Fix: drop the third arg in the single call site at `cdx_ehash.c:3301`. (2) `create_eth_rx_stats_hm()` (decl L108, def L2549–2586) has all four call sites (lines 730/1215/2830/3433) wrapped in `#ifdef INCLUDE_ETHER_IFSTATS`; once ask10 commented that macro out, the function became unreachable and `-Werror=unused-function` fired. Fix: tag both forward decl and definition with `__maybe_unused`. Both edits extend `release/patches/ask-modules/02-cdx-lf66-stubs.patch`. Patch-health remains green: `Pass: 21  Fail: 0  0 SDK conflicts (265 files to install)`.

> **ask13 OOT module fix (2026-05-04):** ask12 CI advanced past `cdx_ehash.c` but failed at `work/ask-oot/src/cdx/devoh.c:519: error: argument 1 null where non-null expected [-Werror=nonnull]` for `dpa_fd_release(NULL, fd);` in the offline-port RX-error fallback path of `oh_dqrr_cb()`. lf-6.6.y `release/patches/kernel/sdk-sources/drivers/net/ethernet/freescale/sdk_dpaa/dpaa_eth_common.h:204-205` declares `dpa_fd_release()` with `__attribute__((nonnull))` (no arglist → both args non-null); lf-6.12.y dropped this attribute, so the cdx caller passing `NULL` for `net_dev` was previously fine. The call is legitimate — at the offline-port stage there is no associated netdev, and `dpa_fd_release()` only dereferences the fd. Fix: extend `release/patches/ask-modules/02-cdx-lf66-stubs.patch` with a fourth hunk that wraps the single `dpa_fd_release(NULL, fd)` call site in `#pragma GCC diagnostic push` / `ignored "-Wnonnull"` / `pop`. No behaviour change. Patch-health remains green: `Pass: 21  Fail: 0  0 SDK conflicts (265 files to install)`.

> **ask14 OOT module fix (2026-05-04):** ask13 CI cleared `devoh.c` but failed at `work/ask-oot/src/cdx/cdx_ceetm_app.c` with four `-Werror=implicit-function-declaration` errors: `FmPcdPlcrProfileGetAbsoluteId` (line 689), `dpa_register_ceetm_get_egress_fq` (lines 863, 1754), `dpa_enable_ceetm` (line 1125), `qman_ceetm_configure_mapping_shaper_tcfc` (line 1344). All four are CEETM TX-QoS shaping helpers exported by the lf-6.12.y SDK that ASK-mono is authored against, but absent from lf-6.6.y. CEETM is not on the boot path / `cmm` startup path; no other cdx TUs call these symbols (`grep` for `cdx_ceetm_app_*` outside `cdx_ceetm_app.c` returns zero matches). Fix: extend `release/patches/ask-modules/02-cdx-lf66-stubs.patch` with a 5th hunk inserting four `static inline` no-op stubs at the top of `cdx_ceetm_app.c` (immediately after the includes), satisfying the implicit-function-declaration check while link-folding the bodies away. Patch-health remains green: `Pass: 21  Fail: 0  0 SDK conflicts (265 files to install)`.

> **ask15 OOT module fix (2026-05-04):** ask14 CI cleared `cdx_ceetm_app.c` but failed at `work/ask-oot/src/cdx/cdx_reassm.c:473: error: implicit declaration of function 'register_dpaa_eth_bpool_replenish_hook' [-Werror=implicit-function-declaration]`. Same lf-6.6.y vs lf-6.12.y SDK ABI gap — the helper that registers a callback for asynchronous IP-reassembly fragment-pool replenishment was added by lf-6.12.y. Without the hook installed, frag-pool replenishment falls back to the synchronous path inside the SDK, which is not exercised on the boot / `cmm` startup path. Fix: extend `release/patches/ask-modules/02-cdx-lf66-stubs.patch` with a 6th hunk inserting a `static inline int register_dpaa_eth_bpool_replenish_hook(void *)` no-op stub at the top of `cdx_reassm.c` (after the includes block). Single call site (line 473), void return ignored by caller, no other TUs reference the symbol. Patch-health remains green: `Pass: 21  Fail: 0  0 SDK conflicts (265 files to install)`.

> **ask16 OOT module fix (2026-05-04):** ask15 CI cleared `cdx_reassm.c` but failed at `work/ask-oot/src/cdx/cdx_qos.c:621` with `implicit declaration of function 'FmPcdPlcrProfileGetAbsoluteId'`. The ask14 fix put the stub as `static inline` inside `cdx_ceetm_app.c` only — it isn't visible to `cdx_qos.c`, which is the second TU that references this lf-6.12.y helper. Both TUs include `cdx_common.h` but the stub was deliberately kept TU-local to avoid header surgery. Fix: extend `release/patches/ask-modules/02-cdx-lf66-stubs.patch` with a 7th hunk inserting the same `static inline uint16_t FmPcdPlcrProfileGetAbsoluteId(void *)` no-op stub at the top of `cdx_qos.c`. `static inline` in two TUs is valid (no linker collision; each TU gets its own private copy). The other 4 ask14 stubs (`dpa_register_ceetm_get_egress_fq`, `dpa_enable_ceetm`, `qman_ceetm_configure_mapping_shaper_tcfc`) verified by `grep` to have call sites only in `cdx_ceetm_app.c`, so they remain TU-local. Patch-health remains green: `Pass: 21  Fail: 0  0 SDK conflicts (265 files to install)`.

> **ask17 OOT module fix (2026-05-04):** ask16 CI cleared `cdx_qos.c` but failed at `work/ask-oot/src/cdx/vsp_cfg.c:71: error: implicit declaration of function '_dpa_bp_free'; did you mean 'dpa_bp_free'?`. Same lf-6.6.y vs lf-6.12.y SDK ABI gap. lf-6.6.y exports `dpa_bp_free(struct dpa_priv_s *)` (`dpaa_eth.c:875,1139`) and `_dpa_bp_free_pf(void *addr)` (`dpaa_eth.h:646`, used as `free_buf_cb`), but no `_dpa_bp_free()` taking a `struct dpa_bp *`. The single call site (`dpa_remove_virt_storage_profile()` releasing `gs_dpa_vsp_bp`) is on the VSP teardown path, not on the boot/cmm path. Fix: extend `release/patches/ask-modules/02-cdx-lf66-stubs.patch` with an 8th hunk inserting `static inline void _dpa_bp_free(void *bp) { (void)bp; }` at the top of `cdx/vsp_cfg.c` (after the include block, before `gs_dpa_vsp_bp` decl). Patch-health remains green: `Pass: 21  Fail: 0  0 SDK conflicts (265 files to install)`.

> **ask18 link-step closure (2026-05-04):** ask17 CI got past every compile error in the cdx OOT module but failed at modpost with 10 unresolved symbols and 1 section-mismatch warning. Each prior askN (ask10..ask17) had patched a single TU's compile error with a `static inline` stub — fine for the compiler, useless at link time because static-inline either inlines (no symbol emitted) or is dropped (internal linkage), so once cdx.ko got linked, modpost saw bare `EXTERN` references to lf-6.12.y SDK helpers that the lf-6.6.y SDK source doesn't define. ask18 closes the gap with a coordinated three-class fix instead of one-more-stub-per-TU: **Class A** (5 syms present in lf-6.6.y SDK but not exported) — new `release/patches/fixes/106-sdk-export-symbols-for-cdx-oot.patch` adds `EXPORT_SYMBOL` to `FM_PORT_PcdPlcrAllocProfiles` (`fm_port.c`), `FM_VSP_Config` + `FM_VSP_ConfigBufferPrefixContent` (`fm_sp.c`), `FM_MURAM_AllocMem` (`fm_muram.c`), and `EXPORT_SYMBOL_GPL` for `qman_sp_enable_ceetm_mode` (`qman_config.c`). **Class B** (2 syms absent in lf-6.6.y entirely: `FM_PORT_SetOhPortOfne`, `get_ip_reassem_info`) and **Class C** (3 EHASH externs deleted by ask6's SDK refresh: `ExternalHashTableAllocEntry`, `ExternalHashTableEntryFree`, `ExternalHashTableFmPcdHcSync`) — new `release/patches/ask-modules/03-cdx-lf66-compat-tu.patch` adds a fresh `cdx/lf66_compat.c` TU with non-static no-op stubs + `EXPORT_SYMBOL` for each, and appends `lf66_compat.o` to `dpa_if_objs` in `cdx/Makefile` (NOT `cdx/Kbuild`, which is deleted by patch 01). Section-mismatch warning addressed by extending `02-cdx-lf66-stubs.patch` with a hunk that drops `__exit` from `cdx_cmdhandler_exit()` in `cdx/cdx_cmdhandler.c` — the function is referenced from non-`__exit` callers, which marked the reference invalid. Patch-health bumps from `Pass: 21` → `Pass: 22` (one new patch in `fixes/`); SDK file count unchanged at 265. None of the stubbed helpers are on the boot/cmm path: `FM_PORT_SetOhPortOfne` is offline-port FNE config, `get_ip_reassem_info` is IP-reassembly metadata fetch, the 3 EHASH externs are external-hash-table CRUD never reached because the SDK-side `FM_PCD_HashTableSet()` doesn't take the EHASH branch in lf-6.6.y. Conceptually ask18 is the structural fix that should have been done at ask10 — replaces the per-iteration whack-a-mole with a single coordinated link-step closure.

> **ask21 modpost closure (2026-05-04):** ask20 CI advanced past compile but stopped at modpost on `cdx.ko` with 10 unresolved symbols. ask18 had set the right structure (Class A real `EXPORT_SYMBOL` in SDK sources; Class B+C compat TU with non-static stubs) but covered only a subset of the references modpost flags once `cdx.ko` is linked, and ask19/ask20 attempts at incremental adds were rejected by `git apply` due to malformed hunk headers. ask21 regenerates both patches from real `diff -u` output and extends each to cover the FULL set from the ask20 CI log: **`fixes/106`** gains 4 more `EXPORT_SYMBOL`s — `SYS_VirtToPhys` (`sdk_fman/.../system/sys_io.c`), `FM_VSP_Init`, `FM_VSP_Free` (`fm_sp.c`), `FM_MURAM_FreeMem` (`fm_muram.c`) — bringing the total to 9 exports across 5 SDK files. **`ask-modules/03-cdx-lf66-compat-tu.patch`** extends `cdx/lf66_compat.c` with 6 more stubs — `ExternalHashSetReasslyPool`, `oh_port_driver_get_port_info`, `FM_PORT_SetOhPortRda`, `ExternalHashTableEntryGetStatsAndTS`, `ExternalHashGetDscpVlanpcpMapCfg` (no-op return -1) and `FmMurambaseAddr` (MURAM virt-base global, NULL-init) — for 11 stubs total. None are reachable on the boot/cmm path: EHASH external-hash CRUD never hits because lf-6.6.y's `FM_PCD_HashTableSet()` does not take the EHASH branch; offline-port RDA / port-info config and IP-reassembly external-pool config are not exercised on this board. Patch-health unchanged: `Pass: 27  Fail: 0  0 SDK conflicts (265 files to install)`.

> **ask23 modpost extension (2026-05-04):** ask22 CI cleared the patch-apply step but modpost on `cdx.ko` flagged 5 NEW unresolved symbols not yet covered by `cdx/lf66_compat.c`: `ExternalHashTableAddKey`, `ExternalHashTableDeleteKey`, `ExternalHashSetDscpVlanpcpMapCfg`, `ipr_update_timestamp`, `fm_port_get_hwid`. Same lf-6.12.y vs lf-6.6.y SDK ABI gap pattern as the symbols already covered: lf-6.6.y SDK does not provide them, callers in `cdx_ehash.c` / `control_socket.c` / `cdx_dpa_ipsec.c` / `cdx_reassm.c` / `devman.c` are on EHASH external-hash CRUD, IPR-tick, and informational hwid paths that are not exercised on the boot/cmm path. Fix: extend `release/patches/ask-modules/03-cdx-lf66-compat-tu.patch` to bring `cdx/lf66_compat.c` from 11 to 16 stubs (file grows from 103 → 143 lines). Each stub gets `EXPORT_SYMBOL` so modpost sees external linkage. Verified locally: `git apply` clean against `we-are-mono/ASK@a211ea865379`, `lf66_compat.c` is 143 lines with 16 EXPORT_SYMBOLs. Patch-health unchanged: `Pass: 27 Fail: 0 0 SDK conflicts (265 files to install)`.

> **ask22 patch-apply fix (2026-05-04):** ask21 CI failed at `git apply 03-cdx-lf66-compat-tu.patch` with "corrupt patch at line 152". Two off-by-one bugs in the patch: (a) the `cdx/lf66_compat.c` new-file hunk header read `@@ -0,0 +1,104 @@` but the body had only 103 `+` lines; (b) the `cdx/Makefile` hunk used spaces for the `cdx_qos.o\` / `vsp_cfg.o\` / `procfs.o` indentation, but the actual upstream file uses two tabs (`\t\t`). `patch-health.sh --source release` had reported clean (`Pass: 27`) because it tests against the kernel tree — the cdx Makefile is not there, only exercised by the OOT applier (`scripts/build-ask-modules.sh`). ask22 regenerates both hunks via real `diff -u` against the post-patch-01 file extracted from `we-are-mono/ASK@a211ea865379`, with tabs preserved. Verified locally: `git apply --include='cdx/Makefile' 01-...patch && git apply 03-cdx-lf66-compat-tu.patch` → `OK applied`, `cdx/lf66_compat.c` 103 lines, `lf66_compat.o` appended to `dpa_if_objs`. Patch-health unchanged: `Pass: 27 Fail: 0 0 SDK conflicts (265 files to install)`.

> **ask6 SDK refresh (2026-05-03):** `release/patches/kernel/sdk-sources/` was re-mirrored from `nxp-qoriq/linux` `lf-6.6.y` @ `e0f9e2afd4cf` (Makefile reports 6.6.52). The previous overlay was a 5.4.3-era drop that carried a broken `USE_ENHANCED_EHASH` machinery (`fm_ehash.c`, `en_exthash_info`, `copy_td_to_ccbase`, etc.) where `FM_PCD_HashTableSet()` returned a `t_FmPcdCcNode *` later reinterpret-cast as `en_exthash_info *`, NULL-derefing `dpa_app` at `copy_td_to_ccbase+0x68` during `FM_PCD_CcRootBuild()`. The lf-6.6.y SDK is what ASK-mono (lf-6.12.y) is authored against and contains no such code path. Net delta: 258 files replaced, 4 EHASH files deleted (`fm_ehash.c`, `fm_ehash.h`, `fm_eh_types.h`, `fm_cc_dbg.h`), 4 carry-overs preserved (`leds-lp5812.[ch]`, `dpaa_ethercat.c`, `fsl_oh_port.h`). Total SDK file count: 266 → 262 → 265 (ask9 restored 3 EHASH headers). Workarounds `fixes/098-fm-cc-ehash-redirect.patch` and `fixes/100-fm-cc-copy-td-null-guard.patch` were obsoleted and removed.

> **ask29 fixes for ask28 CI failures (2026-05-05):** ask28 (commit `d06206d`, tag `kernel-6.6.137-ask28`) failed CI run **25352349418** at the kernel compile step (135s) on TWO new pre-existing `ask-6.6-port` source-port defects in `sdk_fman/Peripherals/FM/`. Both are mechanical fixes to lf-6.6.y SDK files — direct SDK edits per the ask26+ policy. **Fix #1** (`sdk_fman/Peripherals/FM/fm.c`): an orphan `#else ... #endif //AUTO_FIRMWARE_LOAD` block at L3447–3456 had no matching `#if`/`#ifdef AUTO_FIRMWARE_LOAD` opener in the file (the opener was likely deleted in a prior NXP refactor while the trailing branch was left behind). The dead branch referenced a removed `fman_firmware[]` global from `ls1043_r2.h`. Surfaced as `fm.c:3447: error: #else without #if`, `fm.c:3450: 'fman_firmware' undeclared`, `fm.c:3456: #endif without #if`. Fix: deleted the 10 orphan lines and replaced with a single ASK-edit marker comment recording the rationale. **Fix #2** (`sdk_fman/Peripherals/FM/fm_muram.c`): the file uses `kzalloc()` (no `<linux/slab.h>` include), references `FmMurambaseAddr`/`FmMuramsize`/`DBG_UCODE_RESVD_MURAM_SIZE` which are nowhere declared in the TU, and has `EXPORT_SYMBOL(FmMurambaseAddr)` at the bottom — meaning `FmMurambaseAddr` must be defined here non-static. Cross-checked: `fm_ehash.c:40` and `fm_cc_dbg.h:391` declare `extern void *FmMurambaseAddr;`, confirming this TU is the canonical owner. Surfaced as 8 errors (3× `'FmMurambaseAddr' undeclared`, 2× `'FmMuramsize' undeclared`, `implicit declaration of 'kzalloc'`, `'DBG_UCODE_RESVD_MURAM_SIZE' undeclared`). Fix: after the `#include "fm_common.h"` line, add `<linux/slab.h>`, `void *FmMurambaseAddr;`, `static uint32_t FmMuramsize;`, and a `#ifndef DBG_UCODE_RESVD_MURAM_SIZE / #define ... 0 / #endif` guard. Each edit annotated with `/* ASK-edit (ask29): rationale */`. ASK-edit marker count: 14 (ask28) → 16 (ask29). Patch-health remains green: `Pass: 25 Fail: 0 0 SDK conflicts (266 files to install)`. SDK file count unchanged at 266.

> **ask28 fixes for ask27 CI failures (2026-05-05):** ask27 (commit `0cc077b`, tag `kernel-6.6.137-ask27`) failed CI at the kernel compile step with two pre-existing `ask-6.6-port` source-port defects that the SDK-AUDIT review had not flagged. Both are mechanical fixes to lf-6.6.y SDK files — direct SDK edits per the ask26+ policy. **Fix #1** (`sdk_dpaa/dpaa_eth_sg.c`): ask26 hoisted `dpa_get_skb_nh()` above the `CONFIG_INET_IPSEC_OFFLOAD` gate so `cpe_fp_tx()` (under `CONFIG_CPE_FAST_PATH`) could call it, but left the three uapi includes (`<uapi/linux/if_ether.h>`, `<uapi/linux/ppp_defs.h>`, `<uapi/linux/if_pppox.h>`) — which provide `ETH_P_IP/IPV6`, `PPP_IP/PPP_IPV6`, `ETH_P_PPP_SES` — inside the gate. With `CONFIG_INET_IPSEC_OFFLOAD=n` (the producer invariant on 6.6.y), the hoisted function compiled with all those constants undeclared. Surfaced as `dpaa_eth_sg.c:1533: error: 'PPP_IP' undeclared` and `:1535: error: 'PPP_IPV6' undeclared`. Fix: hoist the three uapi includes out of the gate; `<net/xfrm.h>` stays inside (only the still-gated IPSEC helpers below need it). **Fix #2** (`sdk_fman/Peripherals/FM/Port/fm_port.h`): the field at line 947 was named `internalFEBufferPoolManagementAddr` (stale 5.4-era name), but `fm_port.c::FmPortSetFESupport`/`FmPortDelFESupport` (lines 2240, 2244, 2248, 2265, 2297) write/read `internalFEBufferPoolAddr`. The mismatch is a `t_FmPort` struct-member rename that landed in the C file but not the header. Surfaced as 5× `error: 't_FmPort' has no member named 'internalFEBufferPoolAddr'`. Fix: rename the header field to match. Each edit annotated with `/* ASK-edit (ask28): rationale */`. ASK-edit marker count: 12 (ask27) → 14 (ask28). Patch-health remains green: `Pass: 25 Fail: 0 0 SDK conflicts (266 files to install)`. SDK file count unchanged at 266.

> **ask27 SDK-AUDIT P0 implementation (2026-05-05):** Implements the three P0 findings from `SDK-AUDIT-ask26.md` as direct SDK edits per the ask26+ direct-edit policy. **A1** (`fsl_qbman/fsl_usdpaa.c`, 5 sites — `ioctl_id_alloc` ×2, `ioctl_id_release`, `ioctl_id_reserve`, `ioctl_dma_stats`): replaces the `int ret = copy_from_user(...); if (ret) return ret;` (and matching `copy_to_user`) pattern with `if (copy_from_user(...)) return -EFAULT;`. `copy_*_user` returns the count of uncopied bytes (positive on failure), not `-EFAULT`, so the previous code had two boundary defects: (a) success-with-positive-return on syscall failure (userspace would treat as a successful short read of garbage), and (b) `id_alloc`'s copy_to_user error path returned the positive byte-count without releasing the just-allocated FQID/BPID/QPOOL/etc. range — leaking it. **A2** (`sdk_fman/src/wrapper/lnxwrp_ioctls_fm.c:1230`): `ASSERT_COND(num_of_keys <= IOC_FM_PCD_MAX_NUM_OF_KEYS)` was bounding a user-supplied `param->keys_params.num_of_keys` BEFORE the for-loop at L1239 that does `copy_from_user(&keys[k], ...)` and `copy_from_user(&masks[k], ...)`. `ASSERT_COND` compiles out under `DISABLE_ASSERTIONS` (production config), permitting a heap OOB write of attacker-controlled length into `keys[]/masks[]`. Replaced with a runtime `if (... > MAX) { XX_Free(param); return -EINVAL; }` gate. **A3** (`fsl_qbman/qman_driver.c:223,255`): dropped the dead `sp++;` and `lni++;` increments at the end of the CEETM sub-portal / LNI parse loops. Both pointers are reset by the next iteration's `kzalloc()` so the increment was a no-op, but the post-incremented value pointed past the just-linked list-node and any future code change reading `sp->idx` after the increment would have OOB-deref'd. Each edit annotated with `/* ASK-edit (ask27): rationale */`. ASK-edit marker count: 4 (ask26) → 12 (ask26 + ask27). Patch-health remains green: `Pass: 25 Fail: 0 0 SDK conflicts (266 files to install)`. SDK file count unchanged at 266. Audit findings A4/A5/B6/B1/B2/B3/C1/C2/C5/C6 deferred to ask28..ask30 per the sequencing table at the bottom of `SDK-AUDIT-ask26.md`.

> **ask26 direct SDK source edits — policy shift (2026-05-05):** ask25 CI (run 25350444477, 6m59s) failed at the kernel compile step with three `ask-6.6-port` source-port defects: (1) `sdk_fman/src/wrapper/lnxwrp_sysfs_fm.c` carried a stale forward declaration `static int fm_get_counter(void *h_fm, enum fman_counters cnt_e, ...)` referencing a non-existent enum (the real type is `e_FmCounters` defined at line 50); (2) `sdk_dpaa/dpaa_eth_sg.c::dpa_get_skb_nh()` was defined inside a `#ifdef CONFIG_INET_IPSEC_OFFLOAD` gate but called from the `CONFIG_XFRM` branch above — the IPsec offload data path is unconditionally `=n` on 6.6.y, so the function vanished while callers remained; (3) `sdk_fman/Peripherals/FM/Pcd/fm_cc.c::FM_PCD_HashTableSet()` was missing 13 local variable declarations and `FM_PCD_HashTableModifyMissMonitorAddr()` called undefined `ExternalHashTableModifyMissMonitorAddr()`. All three are defects in the NXP `ask-6.6-port` source itself, not 6.6.y kernel API drift. Initial response was three new `fixes/113..115` patches; that approach was rejected because (a) `ask-6.6-port` is a **dead branch** (NXP has stated no further updates will land), so a parallel `fixes/` stack on top of frozen upstream buys zero rebase safety, and (b) the malformed-hunk failure mode (off-by-one `@@` arithmetic — see ask13→14) had already cost a build in this iteration set. Pivot: edit the SDK source files in place under `release/patches/kernel/sdk-sources/...`, annotate every change with `/* ASK-edit (ask26): <rationale> */` markers, and update `.clinerules/20-sdk-driver-rules.md` to formalize the new policy. The grep `grep -rn 'ASK-edit' release/patches/kernel/sdk-sources/` is now the canonical audit surface for "what does ASK change vs upstream NXP". Three direct edits applied: (1) deleted stale forward decl in `lnxwrp_sysfs_fm.c`; (2) hoisted `dpa_get_skb_nh()` and its 4 `#define`s above the `CONFIG_INET_IPSEC_OFFLOAD` gate in `dpaa_eth_sg.c`, reopening the gate immediately after; (3) added the 13 missing locals to `FM_PCD_HashTableSet()` and stubbed `FM_PCD_HashTableModifyMissMonitorAddr()` to `return E_NOT_SUPPORTED;` in `fm_cc.c`. Draft patches `fixes/113..115` deleted before commit. Patch-health remains at `Pass: 25 Fail: 0 0 SDK conflicts (266 files to install)` — drafts `fixes/113..115` were never committed, so deleting them does not change the count. SDK file count unchanged at 266. Commit prefix `sdk-edits:` (distinct from `sdk:` for verbatim re-imports).

> **ask25 SDK refresh #2 (2026-05-04):** `release/patches/kernel/sdk-sources/` was re-mirrored from `nxp-qoriq/linux` branch `ask-6.6-port` @ `6d0b77e999047` ("Port NXP ASK to Linux 6.6.52 (LS1046A)", base tag `lf-6.6.52-2.2.0-1`). This branch is NXP's own ASK port to 6.6, carrying the working EHASH/DDR-offload machinery (`fm_ehash.c`, `USE_ENHANCED_EHASH=1` at `inc/Peripherals/fm_pcd_ext.h:51`) forward from lf-6.12.y. **Restores Chain-2 sub-trigger A's actual fix path**: hash tables can now allocate buckets in DDR rather than 384 KiB MURAM, eliminating `fm_cc.c:4377 AllocStatsObjs Memory Allocation Failed` once the consumer's `cdx_pcd.xml` re-enables `external="yes" aging="yes"`. Net delta vs ask6 base: 71 SDK files updated (sdk_fman/, sdk_dpaa/, fsl_qbman/, headers under `include/linux/` and `include/uapi/linux/fmd/`), 1 file added (`fm_ehash.c`, 1987 lines). Total SDK file count: 265 → **266**. Carry-over `dpaa_ethercat.c` preserved (NXP's tree doesn't carry it). Six `fixes/` patches and two `ask-modules/` compat-shim patches obsoleted by this refresh: `fixes/101` (stdlib_ext fortify port — NXP already removed the prototypes), `fixes/106` (`EXPORT_SYMBOL` adds for `FM_PORT_PcdPlcrAllocProfiles`/`FM_VSP_*`/`FM_MURAM_*`/`SYS_VirtToPhys`/`qman_sp_enable_ceetm_mode` — NXP already exports them all), `fixes/112` (UAPI hashtable extend — `aging_support`/`external_hash`/`miss_monitor_addr` already present), `ask-modules/02-cdx-lf66-stubs.patch` (the `dpaa_eth_refill_bpools` 3-arg signature now matches lf-6.12.y, the 6 TU-local `static inline` stubs would shadow real exports), `ask-modules/03-cdx-lf66-compat-tu.patch` (all 16 stubbed symbols `ExternalHash*`/`FmMurambaseAddr`/`ipr_update_timestamp`/`fm_port_get_hwid`/`FM_PORT_SetOhPort{Ofne,Rda}`/`oh_port_driver_get_port_info`/`get_ip_reassem_info` are now real `EXPORT_SYMBOL`s in NXP's tree — keeping them would produce duplicate-symbol link errors). Patch-health: `Pass: 25 Fail: 0 0 SDK conflicts (266 files to install)`.

SDK source files (266 of them) are dropped under `release/patches/kernel/sdk-sources/` and copied into the kernel tree by `scripts/apply-to-tree.sh`. The 266 includes the lp5812 driver source pair, the dpaa_ethercat carry-over, the fsl_oh_port.h header, and `fm_ehash.c`.

Patches in `fixes/` may target SDK-dropped files. `scripts/patch-health.sh` stages SDK sources into the kernel tree before the dry-run check so such patches validate cleanly.

## Driver Stack (the why behind ASK)

ASK ships **NXP SDK drivers**, not mainline:

| Symbol | Path | Mainline equivalent (NOT used) |
|---|---|---|
| `CONFIG_FSL_SDK_FMAN=y` | `drivers/net/ethernet/freescale/sdk_fman/` | `fman/` (`CONFIG_FSL_FMAN`) |
| `CONFIG_FSL_SDK_DPAA_ETH=y` | `drivers/net/ethernet/freescale/sdk_dpaa/` (fsl_mac, fsl_dpa) | `dpaa/` (`CONFIG_FSL_DPAA_ETH`) |
| `CONFIG_FSL_SDK_DPA=y` | `drivers/staging/fsl_qbman/` | `drivers/soc/fsl/qbman/` |

The SDK is required because mainline doesn't expose USDPAA / FMC / `dpa_ipsec` / `fmlib` userspace ABI consumed by the ASK userspace tools (`dpa_app`, `fmc`, etc).

Known SDK pitfall: `sdk_dpaa/mac.c:202` returns `-ENODEV` (not `-EPROBE_DEFER`) when `fm_bind()` finds FMan not yet probed. Mainline doesn't have this — it uses the component framework. Init order between `sdk_fman/` and `sdk_dpaa/` therefore matters; patch `ask/010`'s Makefile orders `sdk_fman/` first.

## Reference-Aligned Defconfig Invariants

The NXP/ASK 6.12 reference (`work/reference/config/kernel/defconfig`) ships `CONFIG_NET_KEY=y` (built-in), not `=m`. This is **load-bearing** for ASK: the NETLINK_KEY=32 socket that `cmm`'s `fci_open(FCILIB_KEY_TYPE)` requires is created from `af_key.c::ipsec_pfkey_init()`. With `CONFIG_NET_KEY=m`, `net/Makefile` enters `net/key/` via `obj-m += key/` (module-only descent), and any `obj-y` line inside `net/key/Makefile` is silently dropped — including our patch 097's `obj-$(CONFIG_ASK_FCI_NLKEY) += ask_fci_nlkey.o`. The kernel image then has no proto-32 registration, `cmm` fails with `EPROTONOSUPPORT`, and `cmm.service` cycle-restarts to a fake-active state.

Producer-side invariants discovered by ask49 vs reference comparison:

| Symbol | Reference (6.12) | This repo (must match) | Why |
|---|---|---|---|
| `CONFIG_NET_KEY` | `=y` | `=y` (in `release/vyos-base/10-networking.config`) | Required so `obj-y` items in `net/key/Makefile` are honored. |
| `CONFIG_INET_IPSEC_OFFLOAD` | `=y` | **must remain `=n`** on 6.6 | Reference path needs `xfrm_state` fields (`curr_time`, `offloaded`) that don't exist on 6.6.y. Re-enabling fails to compile. |
| `CONFIG_CPE_FAST_PATH` | `=y` | `=y` | ASK fast-path master gate; non-IPsec hooks are guarded by this. |
| `CONFIG_ASK_FCI_NLKEY` | n/a (reference uses NLKEY_SUPPORT inside af_key.c) | `=y` | Our 6.6 narrow-gate that registers proto 32 without pulling in the broken IPsec offload data path. |

ask50 (FCI fix) is therefore a **single-line defconfig change** flipping `CONFIG_NET_KEY=m` → `=y` in `release/vyos-base/10-networking.config`. Patch 097 is already correct.

## Two-chain failure model (post-ask49)

`ask-check` failures collapse into two **independent** chains. Knowing which one a symptom belongs to is critical for routing the fix.

### Chain 1 — kernel-side (this repo's responsibility)
- Symptoms: `cmm process running [FAILED]`, `cmm.service active [FAILED]`.
- Trigger: `socket(AF_NETLINK, SOCK_RAW, NETLINK_KEY=32) = -EPROTONOSUPPORT`.
- Diagnostic checklist on the running device:
  ```bash
  zcat /proc/config.gz | grep -E 'NET_KEY|ASK_FCI_NLKEY|INET_IPSEC_OFFLOAD'
  cat /proc/net/netlink | awk '{print $2}' | sort -u   # must include 32
  grep ask_fci /proc/kallsyms                          # must be non-empty
  ls /sys/module/ask_fci_nlkey 2>/dev/null             # must exist if =y
  dmesg | grep 'ASK FCI'
  ```

### Chain 2 — userspace-side (consumer `vyos-ls1046a-build` responsibility, NOT this repo)
- Symptoms: `dpa_app applied PCD configuration (failed rc=65280)`, `BMan fragment buffer pool located by CDX [FAILED]`, `no ASK driver probe/init/bind failures (≥1 hit(s))`.
- Sub-trigger A (MURAM exhaustion): `fm_cc.c:4377 AllocStatsObjs Memory Allocation Failed`.
  - Cause: `/etc/cdx_pcd.xml` requests ~16K stats objects in 384 KiB FMan MURAM; on-target `fmc` doesn't understand `external="yes" aging="yes"` and silently drops the DDR-offload directives, so hash tables fall back to MURAM and the allocator runs dry.
  - Fix lives in: `vyos-ls1046a-build` (rebuild `fmc`/`fmlib` from a tag that supports `external/aging`, and/or trim `cdx_pcd.xml` key counts).
- Sub-trigger B (NULL-deref in `copy_td_to_ccbase`, ARM64 oops at `+0x68`): the kernel-side path of an EHASH external-hash request was wired wrong in the vendored SDK — `FM_PCD_HashTableSet()` did not dispatch to `ExternalHashTableSet()` when `p_Param->externalHash` was set, so the returned `t_FmPcdCcNode *` was later reinterpret-cast as `en_exthash_info *` and the first field load NULL-deref'd. **Eliminated at producer level by ask6's SDK refresh** (see "ask6 SDK refresh" note above): the lf-6.6.y SDK does not carry the broken `USE_ENHANCED_EHASH` machinery at all, so `copy_td_to_ccbase` simply does not exist in the tree and the fault path is removed. Once a consumer pins ask6 or later, only sub-trigger A's userspace work remains.

The chains are independent: `fci.ko` does not register NETLINK_KEY (that is the in-tree `ask_fci_nlkey` `late_initcall`'s job), and `dpa_app` runs from `cdx_module_init` independent of `cmm`. Diagnose each chain separately and route fixes to the correct repo.

## Useful Commands

```bash
# Local sanity loop
bash scripts/patch-health.sh --source release       # validate patch set
bash scripts/run-pipeline.sh                        # full local build (slow)

# Watch producer CI
gh run list --workflow=build-and-release.yml --limit 5
gh run view <id> --log-failed

# Inspect a published release
gh release view kernel-6.6.137-askN
